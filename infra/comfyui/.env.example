# Copy to infra/comfyui/.env — never commit .env

# Hugging Face token for weight downloads (make fetch-*)
HF_TOKEN=

# comfyui-api webhook HMAC (base64). Must match COMFYUI_WEBHOOK_SECRET in Maya .env
# Generate: openssl rand -base64 32
WEBHOOK_SECRET=
