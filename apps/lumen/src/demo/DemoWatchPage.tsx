import { WatchPage } from '../pages/WatchPage'

/** CINEMAYA-themed watch surface — same race/party logic, cinema player skin. */
export function DemoWatchPage() {
  return <WatchPage variant="cinema" />
}
