{
  "comment": "First-run OpenBao seed (copied to data/openbao/dev-seed.json, not overwritten). Replace slskd with a real Soulseek login and drop network=example to use the live network. Postgres matches docker-compose / GHA pgvector (env DATABASE_URL still overrides).",
  "secrets": {
    "secret/data/maya/integrations/slskd": {
      "username": "maya-dev-example",
      "password": "maya-dev-example",
      "api_key": "ci-cloud-agent-slskd-key",
      "network": "example"
    },
    "secret/data/maya/integrations/postgres": {
      "url": "postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/maya_public",
      "username": "postgres",
      "password": "postgres",
      "host": "127.0.0.1",
      "port": "5432",
      "database": "maya_public"
    }
  }
}
