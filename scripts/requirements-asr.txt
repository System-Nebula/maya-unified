# Dedicated Qwen3-ASR server environment (ASR-001).
# Cannot share the main maya-unified venv: qwen-asr pins transformers==4.57.6
# while faster-qwen3-tts / qwen-tts require transformers==4.57.3.
#
# Create once (CUDA torch first — CPU torch makes ASR too slow for voice):
#   py -3.12 -m venv .venv-asr
#   .\.venv-asr\Scripts\python.exe -m pip install --upgrade pip
#   .\.venv-asr\Scripts\python.exe -m pip install torch==2.7.0 torchanudio==2.7.0 --index-url https://download.pytorch.org/whl/cu128
#   .\.venv-asr\Scripts\python.exe -m pip install -r scripts\requirements-asr.txt
#
# Or: scripts/start-asr.ps1 (bootstraps the same way)
#
# Never use `pip install -U qwen-asr` at launch.
# Do not list bare `torch` here — pip would pull a CPU wheel and ruin latency.

qwen-asr==0.0.6
fastapi>=0.110
uvicorn[standard]>=0.29
soundfile
numpy
