/** Docs embed demo — live set viewer with bundled USB002 tracklist. */
document.addEventListener("alpine:init", () => {
  // mayaLiveSet registers on alpine:init; this file is a no-op placeholder
  // so load order can mirror player-demo (module then demo.js).
});
